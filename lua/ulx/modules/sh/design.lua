VGUI Frame
-----------------
Frame.Paint = function( self, w, h ) -- 'function Frame:Paint( w, h )' works too
	draw.RoundedBox( 0, 0, 0, w, h, Color( 64, 64, 64, 255 ) ) -- Draw a red box instead of the frame
end


Remove buttons color.

Change DNumSlider to a DNumberWang
